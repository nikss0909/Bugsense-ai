# BugSense AI Frontend

React + Vite + Tailwind CSS client for the BugSense AI automated QA platform.

```powershell
npm install
npm run dev
```

Set `VITE_API_URL` if the Spring Boot API is not running at `http://localhost:8080/api`.
